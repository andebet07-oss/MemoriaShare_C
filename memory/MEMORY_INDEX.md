# Memory System Index

Last consolidation: **2026-04-26T22:30Z** (automated — captured 2 new 2026-04-26 PM commits: FRAMES1 photo-print library import + Phase 1/2 of event_permissions sharing feature. The earlier in-repo `upgradeALL_25` consolidation in `dd11a8e` itself committed memory files BEFORE recognizing the new HEAD; this run reconciles.)

Prior consolidation: **2026-04-26T22:00Z** (automated — quiet period: no commits since 2026-04-22 22:43. HEAD unchanged at `9306eee`. Recent-memory horizon stretched 4 days; preserved as-is to retain context until next functional commit.)

Earlier: **2026-04-23T19:00Z** (automated — 2 commits from 2026-04-22 PM not captured by in-repo `upgradeALL_14`: camera quota badge frosted-pill redesign + `Wand2`→`Film` icon swap (`e5f31ec`), UploadManager pending-photos panel full brand re-skin retiring `bg-gray-*` outlier aesthetic (`6643fd2`))

This directory holds structured memory for the Memoria project across sessions.

## Files

- **recent-memory.md** — Rolling 48-hour context (last session's decisions, active tasks, design choices)
- **long-term-memory.md** — Distilled facts, patterns, rules, brand language, tech stack constraints
- **project-memory.md** — Active initiative state, deliverables, testing checklist, known issues

## Critical Facts (at-a-glance)

- **HEAD:** `dd11a8e` (2026-04-26 21:31 +0300) — `upgradeALL_25`: Phase 2 service layer for event-sharing permissions + role-resolution rewrite + new `permissions-${eventId}` realtime channel + agent infrastructure scaffolded. Prior functional commit: `cbd2058` (2026-04-26 21:21) — FRAMES1 photo-print library + Phase 1 RLS migration (`event_permissions` table).
- **EVENT-SHARING ROLLOUT — PHASE 1+2 SHIPPED, PHASES 3-7 PENDING (2026-04-26):** New `event_permissions(event_id, user_id, role IN ('viewer','editor'), granted_by, created_at)` table with UUID-based grants. Migration `20260423_event_permissions_phase1.sql` extends 4 existing photo/event policies with inline `OR EXISTS event_permissions ep ... role='editor'` (no SECURITY DEFINER helper) and adds new `photos_select_viewer` policy. Service layer: `memoriaService.profiles.getByEmail` + `memoriaService.eventPermissions.{getByEvent,getForUser,grant,revoke}`. Role resolver in `useEventGallery.js` + `Dashboard.jsx` rewritten to dual-system priority: admin > creator > new-editor > legacy-cohost > new-viewer > guest. Higher-privilege wins in conflicts. New `effectiveRole` exposed in hook return; `isOwner` retained for back-compat. **Pending:** P3 grant/revoke UI, P4 migrate UI consumers from `isOwner` to `effectiveRole`, P5 fix `CoHostsManager.jsx:29` email-vs-UUID bug, P6 deploy `profiles` SELECT RLS, P7 drop `events.co_hosts` column.
- **NEW REALTIME CHANNEL `permissions-${eventId}` (2026-04-26):** 5th realtime channel in repo (was 4). Re-resolves user's role on grant/revoke without page reload. `invariant_realtime_channels.md` (architect agent) is now stale.
- **RLS NEW PATTERN — INLINE `EXISTS`, NO SECURITY DEFINER (2026-04-26):** Memoria policy from now: editor/membership checks use inline correlated `EXISTS (SELECT 1 FROM <perms> WHERE ... AND user_id = (select auth.uid()) AND role = 'editor')`. **Never** wrap in `is_editor_for_event(UUID)` SECURITY DEFINER — bypasses calling user's RLS context, hidden privilege escalation.
- **DUAL-SYSTEM PERMISSIONS COEXISTENCE RULE (2026-04-26):** During the email-keyed → UUID-keyed migration window (Phases 3-6), every access decision checks BOTH `event_permissions` AND `events.co_hosts[]` with union logic. See long-term-memory §Common Pitfalls "Migrating a sharing/permissions system" for the canonical priority block. The full code template lives in `.claude/agent-memory/03-task-decomposer/arch_role_resolution_pattern.md`.
- **FRAMES1 PHOTO-PRINT LIBRARY (2026-04-26, `cbd2058`):** 30 transparent PNG-32 landscape templates in `public/FRAMES-PROCESSED/` from a sharp-based pipeline (`scripts/import-frames1.mjs`). 28 A2 composite-photo frames pending Figma prep. `frames_meta` constraints extended: new enum values `landscape` + `photo_print`. 30 rows inserted with `status='active'` (verify picker query treats this as canonical). 123 old programmatic frames archived (`minimal_luxury` / `modern_editorial` / `festive_chic`). Vercel CORS extended to `/FRAMES-PROCESSED/(.*)`. New devDep: `sharp@^0.34.5`.
- **AGENT INFRASTRUCTURE SCAFFOLDED (2026-04-26, `dd11a8e`):** `.claude/agents/{01-system-architect,02-impact-analyzer,03-task-decomposer}.md` + `.claude/agent-memory/{memoria-architect,03-task-decomposer}/`. Architect agent dated 2026-04-22 inventoried 7 risks: god files (Dashboard 753, CreateMagnetEvent 741, useEventGallery 741, MagnetLead 615, memoriaService 600, CreateEvent 555), `auth_mutex_workarounds` (memoriaService raw-fetches with localStorage JWT), `triple_array_fanout` (useEventGallery photos/myPhotos/sharedPhotos must mutate together), `useAuth_profileReady_gap`, `orphan_storage_on_db_insert_failure`, `quota_ceiling_duplicated`, `magnet_guest_entry_no_event_type_guard`. To be reconciled with project-memory issues table.
- **LEGACY HEAD (PRE-2026-04-26):** `9306eee` (2026-04-22 22:43) — `upgradeALL_14` (memory consolidation only). Functional code HEAD: `6643fd2` (2026-04-22 22:42) — Redesign UploadManager pending photos panel.
- **ROUTING REFACTOR FALLOUT CLOSED (2026-04-22, `9c0924e`):** `createPageUrl` path-param refactor (`4933138`, 2026-04-20) silently broke share events for two days — every new link resolved `code` to null because `useEventGallery.js`, `Event.jsx`, `EventSuccess.jsx` still read `window.location.search`. Fixed via `useParams()` + query-param fallback. EventSuccess also had malformed share URL (double `?&` from concatenating `&pin=...` onto a no-query path) — rewritten as clean `${BASE_URL}/event/${unique_code}`. See long-term-memory §Common Pitfalls new entry.
- **UPLOADMANAGER BRAND RE-SKIN (2026-04-22 PM, `6643fd2`):** Pending-photos panel now matches cool-dark brand system end-to-end — editorial indigo header (icon badge + `tracking-[0.25em]` micro-label + count pill), `rounded-[14px]` thumbs with depth shadow, compact 28px filter pills with `Film` icon on vintage, indigo-gradient primary CTA `linear-gradient(135deg,#7c86e1,#6368c7)` + glow. Retired generic `bg-gradient-to-br from-gray-900/70 to-gray-800/50` outlier. Follow-up: grep Share side for remaining `bg-gray-*` / `border-gray-*`.
- **CAMERA QUOTA BADGE TRI-STATE (2026-04-22 PM, `e5f31ec`):** `MagnetCamera` quota copy rebuilt as frosted pill with number-first typography (`text-xl font-black tabular-nums`). Tri-state: white normal → `text-amber-400` at ≤3 remaining → red pill `rgba(239,68,68,0.12)` with `המכסה הסתיימה` when exhausted. Vintage filter toggle icon swapped `Wand2` → `Film` (semantically accurate; same icon now used on UploadManager filter pill).
- **PNG FRAME ASPECT PROPAGATION (2026-04-22, `4b38ddf`):** Square PNG frames (`frame.aspect === 'square'`) were being letterboxed in the admin grid + detail panel because `PH` assumed portrait ratio. Fixed by splitting `PH_PORTRAIT` / `PH_SQUARE` in FrameDetailPanel and forwarding `aspect` through `setSelected()` in FramesLibrary.
- **PNG FRAMES PIPELINE SHIPPED (2026-04-21, `d0db4cc`):** Parallel to procedural `drawFrame()`. New primitives: `compositePngFrame.js`, `detectHoleBbox.js` (alpha-channel cutout detection), `FramePngPreview.jsx`, `FrameUploadDialog.jsx`. Hardening rules (crossOrigin only for Supabase URLs; delete failed-image promises from cache on reject; cap canvas to 600×900 in preview; CORS on `/FRAMES/`) documented in long-term-memory §PNG Frame Overlay Pipeline.
- **ADMIN AUTH RACE FIXED (2026-04-21, `276562a`):** `profileReady` state in AuthContext gates role-enrichment completion. `RequireAdmin` now requires both `!isLoadingAuth && profileReady`. 6s enrichment timeout + 10s whole-auth safety timer. See long-term-memory §Admin Auth Race Pattern.
- **FRAME LIBRARY EXPANDED (2026-04-21):** 7 AI-designed SVG seeds + 8 Figma transparent PNGs + 71 Canva polaroids = 86 frames. Placeholder SVG seeds purged. Sources: `06c353e`, `f7def4d`, `4e73962`, `c1df70f`.
- **STICKER PACKS `emoji` TYPE PROMOTED (2026-04-21, `5d13611`):** First-class alongside `svg`/`script-text`/`retro-text`/`editorial-text`. Wedding pack 15 → 35+ stickers. Hebrew content: `מזל טוב`, `בר מצווה`.
- **ADMIN PANELS BRAND-ALIGNED (2026-04-21, `0f094a8`):** AdminEventsList + LeadsPanel hardcoded `rgba(...)` → `bg-card` / `border-border` / `text-muted-foreground`. Editorial headings now `font-playfair`. New banned pattern enforced on admin side.
- **LAYOUT.JSX DELETED (2026-04-20, `4933138`):** `.luxury-button` and `.premium-submit-button` retired. Use `<Button>` from `@/components/ui/button`. Grep for stale refs.
- **SHARED STATE COMPONENTS (2026-04-20):** `src/components/ui/LoadingState.jsx`, `ErrorState.jsx`, `EmptyState.jsx` are canonical. Never hand-roll a spinner or error block in a page.
- **TAILWIND.CONFIG.JS (2026-04-20):** Canonical source for custom animations (`animate-paper-fly`), extended colors, `shadow-indigo-soft`.
- **MAGNETCAMERA HARDENED (2026-04-20, `c0d6cfd`):** Cancellation token, centralized setTimeout tracking, in-app UA fallback, video-ready guard, GPU-first vintage filter, Hebrew aria-labels, retry button, escape-to-close, haptic on quota-exhaust. See long-term-memory §MagnetCamera Hardening Patterns.
- **ARIA CONVENTIONS (2026-04-20):** Tab semantics, Hebrew aria-labels, focus-visible rings, `role="alert"`, `aria-pressed` on toggles, modal surfaces `role="dialog" aria-modal="true"`. See long-term-memory §Accessibility Conventions.
- **BRAND LOCKED (2026-04-17, refined 2026-04-19, softened 2026-04-20):** POV.camera cool-dark. Background `#1e1e1e`, primary `#7c86e1`, text `#fcfcfe`. Playfair Display + Heebo + Montserrat editorial labels.
- **VIOLET SUB-BRAND SCOPE:** Admin back-office + in-event operational surfaces only. Consumer intake = indigo/primary. MagnetGuestPage header premium badge removed `96dbbbe` — full re-audit pending.
- **SEGMENTED-CONTROL PATTERN (2026-04-19):** Parent `rounded-2xl bg-secondary border border-border` + transparent children marked by `border-primary shadow-indigo-soft`.
- **STICKER SYSTEM V2 (2026-04-17 PM):** Y2K / Pinterest aesthetic. 5 types in `stickerPacks.js`. 24 SVG stickers with white die-cut stroke in `svgStickers.js`. Emoji now 1st-class (2026-04-21).
- **MAGNETREVIEW PREVIEW COMPOSITE (2026-04-18):** Review screen bakes photo + frame + label to `previewUrl` data URL. `photoFrac` constrains sticker drag zone. **Rule:** at submit, `drawSticker(ctx, s, photoW, photoH, ...)` — NEVER `canvas.height`.
- **SHADOW UTILITY:** `shadow-indigo-soft` = `0 4px 20px -4px rgba(124,134,225,0.25)`.
- **OPEN TECH DEBT (HIGH):** event-sharing rollout Phases 3-7 (NEW 2026-04-26), `onAuthStateChange` deadlock audit (auth context touched `276562a`), `linked_event_id` migration, RLS DELETE audit. (MEDIUM): architect-flagged risks reconciliation (NEW 2026-04-26 — god files, auth mutex workarounds, triple-array fanout, profileReady gap, orphan storage non-atomic, quota duplication, magnet guest entry guard), archived programmatic frames audit (NEW 2026-04-26), `min-h-screen` → `min-h-dvh` bulk replace, duplicate `compressImage` in MagnetLead, canvas fonts not gated on `document.fonts.ready`, `events.cover_image` not displayed on guest landing, MagnetGuestPage violet re-audit. (LOW — NEW 2026-04-26): `frames_meta` status='active' vs 'approved' verification, `invariant_realtime_channels.md` stale (4 → 5), sharp native binding on Vercel build, 28 A2 frames pending Figma prep. (LOW — 2026-04-22): dangling `window.location.search` reads after path-param refactor, remaining `bg-gray-*` Share-side tokens. See project-memory.md.
